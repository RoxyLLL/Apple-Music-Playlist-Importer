# 曲库检索批量显示“Apple Music 曲库未收录”解决方案

## 1. 问题定义

截图中连续多首歌曲都显示“Apple Music 曲库未收录”，但该文案**不能证明歌曲确实未上架**。对一组包含常见中文歌、日文歌的曲目出现集中未命中时，应优先视为“检索链路异常或受限”，而不是逐首歌曲都不存在。

当前产品将以下完全不同的情况都显示成同一句“Apple Music 曲库未收录”：

| 实际结果 | 当前页面表现 | 是否可以称为“未收录” |
| --- | --- | --- |
| Apple Music 正常返回 200，所有检索词均无歌曲结果 | 曲库未收录 | 可以 |
| Apple 返回 401 / 403（开发者 Token 或授权失效） | 曲库未收录 | 不可以 |
| Apple 返回 429（请求过快、被限流） | 曲库未收录 | 不可以 |
| 网络连接、DNS、TLS 或读取超时 | 曲库未收录 | 不可以 |
| Apple 返回 5xx、响应格式异常 | 曲库未收录 | 不可以 |
| 找到候选，但评分低或版本冲突 | 曲库未收录 | 不可以，应显示“候选待确认” |

因此，本次目标不是简单调低匹配分数，而是让系统先能可靠地区分“无结果”和“检索失败”，再降低批量检索被限流的概率。

## 2. 现有代码中的根因

### 2.1 错误被吞掉后转换为空数组

`applemusic/client.py` 的 `search_catalog()` 在以下情况最终都返回 `[]`：

- HTTP 429 经三次短暂重试仍未成功；
- HTTP 401 刷新开发者 Token 后仍失败；
- 其他 HTTP 状态码；
- 任意网络或 JSON 处理异常。

`applemusic/matcher/engine.py` 只根据“候选列表是否为空”作判断，因此会把该空数组生成 `no_match`，理由为“在 Apple Music 曲库中未检索到任何相关结果”。真实错误原因已经丢失。

### 2.2 前端把所有“没有已选候选”的状态渲染为未收录

`applemusic/web/static/index.html` 在行项目没有 `selected_candidate` 时，无论 `decision` 或 `decision_reasons` 是什么，均显示红色的“Apple Music 曲库未收录 / 未找到”。这会把后端的检索异常、评分不足和确实无结果混为一谈。

### 2.3 请求峰值容易触发 Apple 限流

当前一次网页匹配会以 15 首为一批提交给 `/api/match`；后端每批使用 3 个工作线程。一首歌最多会发出 ISRC 查询以及 Tier 1–4 多组关键词查询，且只要尚未达到“自动采纳”阈值就继续扩展检索词。也就是说，一批普通歌曲就可能在短时间内向 Apple Catalog Search 发出数十个请求。

客户端虽然有 429 的共享冷却时间，但首波请求没有节流；429 后的退避也较短，失败最终被伪装为正常空结果。单曲/批量“重试”还会扩大查询词和跨区检索范围，故在受限时可能让问题更明显。

## 3. 解决原则

1. **只有 Apple 正常完成检索且所有查询均无结果时，才标记为“曲库未收录”。**
2. **任何调用失败必须保留机器可读错误码和对用户有用的提示；不得降级为空列表。**
3. **限流应在唯一的 Apple Music 客户端出口统一控制，不能依赖网页批次大小或固定 `sleep`。**
4. **候选不足与没有候选是两种状态。** 找到相近版本但未通过自动匹配时，应让用户选版本，而不是推荐 B 站补漏。
5. **失败可恢复、空结果可缓存。** 短时失败和 429 不能写入“未收录”缓存，也不应阻止后续重试。

## 4. 推荐改造方案

### 阶段 A：先纠正结果语义与页面提示（P0）

#### A.1 为 Catalog 查询建立结构化结果

将 `AppleMusicClient.search_catalog()` 的返回类型从单纯的 `List[AppleMusicTrack]` 改为，例如：

```python
class CatalogSearchOutcome(BaseModel):
    kind: Literal[
        "ok", "no_hits", "rate_limited", "auth_failed",
        "network_error", "timeout", "upstream_error", "invalid_response"
    ]
    tracks: list[AppleMusicTrack] = []
    http_status: int | None = None
    retry_after_seconds: float | None = None
    request_id: str | None = None
    safe_message: str | None = None
```

处理规则：

- HTTP 200 且歌曲列表为空：`no_hits`；HTTP 200 且有歌曲：`ok`。
- HTTP 401/403：`auth_failed`，停止该任务后续请求，不再继续把每首歌都查一遍。
- HTTP 429：`rate_limited`，读取并优先遵守 `Retry-After`；没有该响应头时使用指数退避加随机抖动。
- 连接错误、超时、5xx、JSON 解析失败：分别返回对应失败类型，保留安全的诊断信息。
- 不得在 `except Exception` 中静默返回 `[]`；至少要记录异常类型、请求阶段和 HTTP 状态。

ISRC 查询也必须使用同一套结果语义，避免“ISRC 失败后继续盲查”或“ISRC 异常被误判为无歌曲”。

#### A.2 聚合单首歌的查询结果

匹配引擎应记录每个查询词的结果，并按以下优先级输出最终状态：

1. 有可用候选：`matched` 或 `review`；
2. 所有请求均成功、全部为 `no_hits`：`no_match`；
3. 没有候选且至少一个请求失败：`search_error`；
4. 收到 401/403：`auth_required`；
5. 收到 429 且本轮未能完成：`rate_limited`。

若一首歌已有部分候选，但后续扩展查询失败，应保留已找到的候选，并附上“搜索未完全完成”的提示；不能清空已有结果。

建议将 `SongMatchResult` 增加 `search_status`、`search_attempts`、`search_failures`、`retry_after_seconds` 字段，并让 API 原样返回。`decision` 继续只描述匹配决策，不承担网络健康状态。

#### A.3 让前端按真实状态展示

页面行项目至少区分以下状态：

| `search_status` | 主文案 | 操作 |
| --- | --- | --- |
| `no_match` | Apple Music 当前区域未找到匹配歌曲 | 重试、换区域、搜曲库、B 站补漏 |
| `review` | 找到候选，等待确认 | 查看/更换候选；不显示 B 站作为主操作 |
| `rate_limited` | Apple Music 暂时限制检索，预计 XX 秒后可重试 | 自动倒计时重试、停止批量请求 |
| `auth_required` | Apple Music 授权已失效，请重新登录 | 跳转授权；不再提交其他歌曲 |
| `network_error` / `timeout` / `upstream_error` | 检索服务暂不可用，尚未判断是否收录 | 重试；显示简短错误详情 |

页面汇总不应只显示“未找到 X 首”，应至少显示：`已匹配 / 待确认 / 确认无结果 / 可重试失败 / 未处理`。仅把“确认无结果”的项目送入批量 B 站补漏。

### 阶段 B：在请求出口实施自适应节流（P0）

#### B.1 在 `AppleMusicClient` 内加入全局调度器

为所有 Catalog Search 和 ISRC 请求共用一个线程安全的令牌桶（或单一请求队列）。初始并发和速率应使用保守的可配置值，并根据真实 429 情况动态收紧；不要把未经 Apple 明确公开的具体 QPS 写死为“官方上限”。

建议行为：

- 初始只允许少量在途请求；
- 每次请求发送前必须向调度器申请额度；
- 收到 429 后，暂停整个客户端的发送窗口，而非只暂停当前线程；
- 优先采用 `Retry-After`；无该字段时指数退避（例如 1、2、4… 秒）并加入抖动；
- 连续多次 429 时进入短路状态：当前批次暂停并把未执行曲目标记为“等待重试”，避免把压力继续施加给 Apple；
- 请求成功后缓慢恢复速率，不能立即恢复到峰值。

这应替代目前前端固定 200ms 延时的保护措施。前端延时只能控制一个入口，无法覆盖手动搜索、重试、跨区搜索和未来新增接口。

#### B.2 控制每首歌的搜索预算

建议顺序：

1. 有合法 ISRC 时先精确查一次；
2. 使用“歌曲名 + 主艺人”作为第一检索词；
3. 仅在第一步成功返回但无可用候选时，依次使用精简标题、协作艺人、标题单独搜索；
4. 每首歌曲设置最大请求数和总 deadline；超出预算时输出 `search_incomplete`，而不是 `no_match`；
5. 重试任务只重试失败项。跨区检索应作为用户明确选择或最后一级降级，不能在限流期间自动扩大到多个区域。

常用的、正常返回的“空结果”可按区域和规范化检索词短期缓存；网络错误、401、429、5xx 和超时绝不缓存为负结果。

### 阶段 C：认证预检与可观测性（P1）

#### C.1 批量开始前执行一次健康检查

在解析歌单后、提交第一批匹配前，执行一次轻量预检：

- `media-user-token` 是否存在且验证通过；
- 开发者 Token 是否能取得；
- 对当前 storefront 的一个固定、低成本查询是否成功；
- 当前调度器是否正处于 429 冷却期。

预检失败时不启动整张歌单的匹配，并直接告诉用户“授权异常”“网络不可用”或“Apple 暂时限流”。这样可以避免截图这种几十首连续被误标的情况。

#### C.2 记录可排查但不泄密的诊断数据

每个批次记录：开始/结束时间、storefront、曲目数、查询数、200 空结果数、200 有候选数、401/403、429、5xx、超时数、平均及 P95 延迟、退避次数。错误日志中不得记录 Developer Token、Music-User-Token、完整请求头或个人资料库内容。

提供一个“导出诊断摘要”按钮或在匹配结果 CSV 中增加 `search_status`、`http_status`、`attempt_count`、`retryable` 字段，方便用户反馈问题。

## 5. 实施顺序

1. 修改数据模型和客户端返回值，保留 API 异常原因；补齐客户端单元测试。
2. 修改匹配引擎的结果聚合规则，确保错误永远不会变成 `no_match`。
3. 修改 `/api/match`、`/api/rematch` 与前端渲染；先上线状态区分与授权/限流提示。
4. 接入客户端级令牌桶、`Retry-After`、指数退避和短路；移除对前端固定延时的依赖。
5. 增加批量前预检、指标、诊断导出和端到端回归测试。

为降低风险，第一版可以保留原有匹配评分与阈值，只改“请求结果语义 + 展示 + 节流”。确认不再误报后，再根据真实统计数据微调检索词与评分策略。

## 6. 验收标准

### 功能验收

- 模拟 HTTP 200 且所有关键词都为空时，曲目显示“当前区域未找到匹配歌曲”，并允许换区/手动搜索/B 站补漏。
- 模拟 401、403、429、超时、网络断开、5xx 和非法 JSON 时，任何曲目都不得显示“曲库未收录”；必须展示可重试或重新授权状态。
- 一首歌已有候选、后续查询超时时，已有候选仍可供用户确认。
- 批量中发生授权失败后，尚未开始的歌曲不再向 Apple 发请求，并显示“未处理：需重新授权”。
- 批量中发生 429 后，未执行曲目显示“等待重试”，恢复后只重试这些曲目；不会把它们送入 B 站补漏。

### 稳定性验收

- 在 100 首歌的受控测试中，模拟首波 429 后请求速率明显下降，且无持续突发重试。
- 相同检索词并发到达时只发出一次上游请求；等待者取得同一结构化结果（包括失败结果）。
- 负缓存只包含经过完整成功检索的 `no_hits`，网络/限流错误不会污染下一次搜索。
- 结果统计中，“确实无结果”与“检索失败”的总数可分别核对，二者不再混合。

## 7. 用户临时处理建议

在改造完成前，遇到截图所示的连续未收录，请不要直接批量使用 B 站补漏。建议先：

1. 查看授权状态并重新登录 Apple Music；
2. 等待一段时间后只对其中一首常见歌曲点“搜曲库”验证；
3. 若单曲能搜到，说明原批次更可能是限流/请求失败，应分小批重试；
4. 仅在手动搜索和跨区搜索均正常返回空结果后，再确认该曲目确实需要本地补漏。

这份方案的核心是：**先证明“查成功了但没有”，再告诉用户“曲库未收录”。**
