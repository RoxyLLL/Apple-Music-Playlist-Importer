# Apple Music 对应歌曲检索优化方案

> 项目：AppleMusicSync  
> 分析日期：2026-09-13  
> 范围：仅分析与方案设计；未修改任何现有 Python、HTML、配置或打包文件。本文是本次新增的设计文档。

## 结论摘要

项目已具备完整的歌单迁移链路：从网易云、QQ 音乐、Spotify 或本地文件提取曲目，调用 Apple Music Catalog Search 获得候选，再按歌名、歌手和时长评分。

当前的主要瓶颈不是“缺少模糊匹配”，而是候选召回不足、版本语义被清洗丢失、搜索过早停止，以及自动采纳的置信度没有校准。优先建议如下：

1. P0：保留并识别 Live、Remix、伴奏、翻唱、Demo、Remaster、Explicit 等版本信息；禁止把它们当普通噪声无条件删除。
2. P0：改造停止条件和自动采纳规则。出现 0.60 的候选不代表已经找到可靠答案；应同时满足分数、第一二名分差、版本一致性和关键字段校验。
3. P1：增加分层召回与元数据利用。优先使用 ISRC；其次为歌名、主艺人、专辑和时长；各层候选统一重排。
4. P1：统一阈值、可观测性和回归集，以“自动导入准确率”而不是“匹配数量”作为核心指标。
5. P2：完善动态限流、持久缓存与人工确认闭环，提升大歌单稳定性和复跑速度。

## 项目现状解析

### 架构与数据流

~~~mermaid
flowchart LR
    A[外部歌单 / 本地文本] --> B[extractors]
    B --> C[Track: 歌名、歌手、专辑、时长、ISRC]
    C --> D[TextCleaner]
    D --> E[MatchingEngine]
    E --> F[AppleMusicClient Catalog Search]
    F --> G[候选去重]
    G --> H[TrackScorer]
    H --> I[自动选择 / 人工确认]
    I --> J[创建 Apple Music 歌单并写入]
~~~

| 模块 | 当前职责 | 对检索质量的影响 |
| --- | --- | --- |
| applemusic/extractors/ | 解析网易云、QQ 音乐、Spotify、TXT/CSV | 决定可用于精确匹配的原始元数据是否齐全 |
| applemusic/models.py | Track、AppleMusicTrack、候选结果模型 | 已预留 album、duration_ms、isrc，但没有贯穿检索决策 |
| applemusic/matcher/cleaner.py | 标题规范化、生成搜索词 | 清洗过强，容易删除应保留的版本意图 |
| applemusic/matcher/engine.py | 调用搜索、合并候选、触发评分 | 仅两轮搜索，并在较低分数时提前结束 |
| applemusic/matcher/scorer.py | 歌名、歌手、时长综合评分 | 没有专辑、ISRC、版本类型、结果分差等特征 |
| applemusic/client.py | Catalog Search、缓存、重试 | 有基础缓存和连接池，但缺少精细失败分类、动态节流与更完整候选元数据 |
| applemusic/web/ | 批量匹配及人工替换界面 | 可展示候选，但手工搜索结果没有真实评分和置信度解释 |

### 当前匹配行为

1. 来源曲目进入 Track。网易云和 QQ 音乐通常提供歌名、歌手、专辑和时长；Spotify 的部分路径还能提供 ISRC；本地文本通常只有歌名和歌手。
2. TextCleaner.generate_search_queries() 只产生两类词：清洗后歌名 + 第一位歌手，及清洗后歌名。
3. 每个搜索词调用 Catalog Search，默认仅取 5 条歌曲候选。
4. 某一轮只要出现任一候选评分大于或等于 0.60，就停止后续搜索。
5. 候选按 歌名 60% + 歌手 40% + 时长修正 排序；达到 min_score（默认 0.55）即被选中。
6. Web 端把曲目分块后顺序提交；每个请求内部才使用 3 个匹配线程。

## 已发现的问题与优先级

| 优先级 | 观察到的事实 | 风险 | 优化方向 |
| --- | --- | --- | --- |
| P0 | clean_title() 会递归移除所有圆括号、方括号与【】内容，Live、Remix、伴奏等也会被移除 | 同名不同版本时，现场版可能自动配成录音室版，Remix 可能配成原版 | 将核心歌名和版本标签拆开保存；版本信息参与搜索和排序 |
| P0 | MatchingEngine 在任一候选达到 0.60 时终止搜索；默认最低入选分数是 0.55 | 中等质量的第一轮结果阻止更准确的后续查询，影响艺人别名、合唱、译名 | 用可自动采纳的决策条件取代单一分数作为停止条件 |
| P0 | 模型注释、评分器和 CLI 显示的置信度阈值并不一致 | 同一结果在 API、界面和命令行可能被解释为不同等级 | 只维护一份配置化阈值，所有端复用同一决策结果 |
| P1 | 每首歌最多两种查询、只使用第一位艺人、候选上限为 5 | 合作艺人、艺人分隔格式差异、短歌名和同名歌曲的召回率偏低 | 按严格到宽松的多层查询收集 10–20 个去重候选 |
| P1 | Track 有 ISRC、专辑、时长字段，但主流程未将 ISRC/专辑用于召回或核心评分 | 失去最可靠的精确键和同名消歧信息 | ISRC 作为第一优先级；专辑、时长和发行信息进入重排序 |
| P1 | Catalog 返回的歌手仅按英文逗号拆分 | &、feat.、/、顿号等格式无法稳定表达主艺人和合作艺人 | 保留 Apple 原始艺人串，统一解析主艺人、合作艺人和别名 |
| P1 | 手动搜索其它版本时，前端把结果固定显示为 0.9，选择后标为 exact | 用户选择被记录成伪精确匹配，导出的质量不可解释 | 单列 user_confirmed 状态；后端返回真实特征和分数 |
| P2 | 429 仅采用短暂固定退避，未按错误类别记录 | 大歌单下可能反复限流，暂时性失败可能被记成未收录 | 自适应限流、指数退避、抖动、熔断和可重试队列 |
| P2 | 缓存仅存在内存，15 分钟 TTL，容量超限整体清空 | 重启后重复请求，热门查询会被不必要淘汰 | 持久化 TTL + LRU 缓存，区分空结果、失败与成功 |
| P2 | 没有可审计的标注样本和质量指标 | 无法证明新规则是在减少错配还是只增加匹配数 | 建立回归集、灰度对比和自动采纳准确率指标 |

## 目标检索设计

### 1. 将清洗改为结构化解析

不要把原始标题替换成被大量删减的字符串。每个来源曲目应保留原始字段，并派生以下检索字段：

| 字段 | 示例 | 用途 |
| --- | --- | --- |
| title_raw | 晴天 (Live at Tokyo Dome) | 原始展示、人工复核 |
| title_core | 晴天 | 主标题精确与模糊比较 |
| version_tags | live、tokyo dome | 版本一致性、版本查询、冲突惩罚 |
| artist_primary | 周杰伦 | 主艺人强约束 |
| artist_featured | 某合作歌手 | 合作艺人加分，不应覆盖主艺人 |
| album_normalized | 叶惠美 | 同名歌曲消歧 |
| duration_ms | 269000 | 版本筛选与时长特征 |
| isrc | 来源的 ISRC | 最优先的精确匹配键 |
| source / source_id | spotify / 原平台曲目 ID | 缓存、调试、反馈闭环 |

版本规则建议：

- Live、Remix、Acoustic、Instrumental、Karaoke、Demo、Edit、Remaster、Mono/Stereo、Explicit/Clean、Sped Up/Slowed 等应识别为结构化标签。
- 来源没有版本标签时，可接受 Remaster 等轻微发行差异，但不应默认接受 Live、Remix、伴奏或 Cover。
- 版本标签冲突，例如来源为 Live、候选为 Studio，应触发强惩罚或人工确认；不能因标题核心一致而自动通过。
- 括号内的城市、录制场次、年份可从核心标题移除，但仍应保存在标签中用于查询和展示。

### 2. 分层候选召回

每层将候选按 Apple Music ID 去重后合并。只有满足高可信自动采纳条件时才可提前结束；来源字段缺失时可跳过相应层。

~~~mermaid
flowchart TD
    A[输入 Track] --> B{有 ISRC?}
    B -- 是 --> C[ISRC 精确检索或验证]
    B -- 否 --> D[严格：核心歌名 + 主艺人 + 专辑]
    C --> E{已通过硬校验?}
    E -- 是 --> Z[自动采纳]
    E -- 否 --> D
    D --> F[标准：核心歌名 + 主艺人]
    F --> G[扩展：核心歌名 + 全部艺人 / 艺人别名]
    G --> H[宽松：核心歌名 / 别名 / 译名]
    H --> I[合并候选并统一重排序]
    I --> J{分数、分差、版本一致性均通过?}
    J -- 是 --> Z
    J -- 否 --> K[人工复核或未匹配]
~~~

| 层 | 查询与候选策略 | 使用条件 | 目的 |
| --- | --- | --- | --- |
| L0 | ISRC 精确匹配；先在实际 Apple Music API 权限与端点约束下验证可用方式 | 来源有 ISRC | 最大限度消除同名、翻唱和译名歧义 |
| L1 | title_core + primary artist；候选额外比对专辑、时长、版本 | 所有来源 | 主路径，强调精度 |
| L2 | title_core + 全部主艺人/合作艺人，及艺人分隔变体 | 合作曲、艺人拆分异常 | 提高合作曲召回 |
| L3 | title_core、规范化别名、罗马音/中文译名别名 | L1/L2 不足或得分接近 | 处理跨语言和平台命名差异 |
| L4 | 仅对未确认曲目按备用 storefront 检索 | 用户明确允许且目标区可写入时 | 覆盖地区曲库差异，不绕过最终可导入性校验 |

说明：L0 的具体调用应以 Apple 当前官方接口和本项目实际 token 权限进行小样本验证后确定。若无法直接按 ISRC 过滤，仍可把它作为候选验证键和高权重特征，不能伪造精确检索成功。

### 3. 候选重排序与决策分离

将候选排序和是否自动选择拆开：前者负责把最可能结果排在前面，后者决定是否可无提示写入歌单。

| 特征 | 建议作用 |
| --- | --- |
| ISRC 完全一致 | 最高优先级；通常可直接通过，但仍展示版本信息 |
| 核心歌名相似度 | 主特征；短标题需要更严格的艺人/专辑佐证 |
| 主艺人匹配 | 强约束；主艺人低匹配时不应因标题相同自动导入 |
| 合作艺人覆盖 | 加分项；缺少合作艺人不必直接否决 |
| 专辑相似度 | 同名、影视原声和不同发行版的有效消歧字段 |
| 时长差 | 连续衰减；不建议只分 3、10、20、45 秒几个台阶 |
| 版本标签一致性 | Live/Remix/Cover/Instrumental 冲突应强惩罚 |
| Explicit/Clean、语言、曲目号、发行年份 | 有数据时作为次级消歧特征 |
| 查询来源层级 | L0/L1 可给小幅先验，但不得压过字段冲突 |

推荐的自动决策规则：

| 结果 | 条件 |
| --- | --- |
| 自动采纳 | ISRC 相同；或总分达到高阈值、第一二名分差足够、主艺人一致、无版本冲突 |
| 建议采纳，待确认 | 总分较高但分差小；或存在专辑/版本差异；或来源元数据不完整 |
| 未匹配 | 没有候选通过最低阈值，或主艺人/版本出现硬冲突 |

第一二名分差不可省略。0.88 对 0.86 的同名候选，即使第一名绝对分较高，也应提示用户；0.93 对 0.68 则可安全得多。阈值不应散落在多个文件，必须集中配置并通过标注集校准。

### 4. 多 storefront 与可导入性

建议把策略拆开：

1. 目标 storefront：默认只在最终写入地区检索，确保命中的 Catalog ID 可直接导入。
2. 辅助 storefront：只在用户显式开启扩大检索范围后用于发现候选和解释其他地区存在，不能在未验证可导入前自动加入歌单。
3. 结果展示：明确标记当前地区可导入、仅其他地区可见、需要人工确认，避免将搜得到误认为能写入。

## 性能、稳定性与体验

## 检索速度与检索精度专项优化

本节把前文原则收敛为可实施的优化方案。速度与精度不能用同一个单一参数换取：盲目提高并发会触发限流，盲目扩大每首歌的查询次数会降低吞吐；盲目降低阈值则会把错误歌曲写入歌单。正确做法是让高确定性曲目走极短路径，让歧义曲目在有限预算内扩大召回并进入复核。

### 优化目标与约束

| 维度 | 目标 | 约束 |
| --- | --- | --- |
| 自动导入准确率 | 自动采纳的结果优先达到 ≥99% | 宁可把有歧义的条目交给确认，也不能静默错配 |
| 网络请求数 | 每个唯一曲目尽量只需 1 次目录请求；复杂曲目最多 3 次 | 不把网络错误或限流结果当作未收录 |
| 重复歌单性能 | 已搜索或人工确认的曲目尽可能本地命中 | 缓存键必须包含 storefront、元数据指纹和规则版本 |
| 大歌单稳定性 | 在 Apple 限流状态下平稳降速、可恢复 | 并发由统一调度器控制，不能由 Web 和后端各自放大 |
| 可解释性 | 每次结果能说明为何自动采纳、复核或拒绝 | 保留原始字段、候选特征、查询路径和错误类别 |

### 精度优化：先建立可靠的判断边界

#### A. 修正缺少元数据时的虚高分

现有评分在来源没有歌手时会给歌手相似度 0.60 的中性分。若标题恰好完全一致，仍可能得到较高总分。这对短标题和同名歌曲尤其危险。

优化规则：

1. 缺少字段时不赋予中性分来抬高分数；只对实际存在且可比较的特征计算分数，并对权重重新归一化。
2. 计算 metadata_adequacy，即来源曲目的信息完整度。只有歌名时，任何结果均不可自动采纳；有歌名和主艺人才能进入正常自动决策；有专辑、时长或 ISRC 时可提高可信度。
3. 对短标题加严。中文核心标题不超过 2 个字符，或英文数字标题不超过 4 个字符时，除 ISRC 外必须同时满足主艺人近似完全一致，并至少有专辑或时长佐证，才允许自动采纳。
4. 主艺人低匹配、版本冲突、候选无有效标题时直接拒绝自动采纳，而不是依赖其他字段抵消。

#### B. 使用可解释的动态权重

以下是可用于首版标注集校准的权重起点，并非最终固定值：

| 特征 | 满足时的最大权重 | 缺失时处理 |
| --- | --- | --- |
| 核心歌名 | 0.34 | 歌名是必填；为空则不匹配 |
| 主艺人 | 0.29 | 缺失时不加中性分，并提高自动门槛 |
| 合作艺人 | 0.06 | 仅作为加分，不因缺失直接扣分 |
| 专辑 | 0.12 | 缺失后将可用权重重新归一化 |
| 时长连续相似度 | 0.09 | 缺失后将可用权重重新归一化 |
| 版本标签一致性 | 0.10 | 来源无标签时对明显特殊版本施加轻度惩罚 |

ISRC 完全一致是独立硬信号，应优先于加权评分。若存在版本冲突或目录数据异常，可改为人工确认而不是直接导入。

时长建议使用连续衰减，例如时长差越大，贡献越低；不要再只使用几个离散档位。这样可避免 11 秒和 19 秒差异被同等处理，也更容易基于样本校准。

#### C. 将候选排序与自动决策分开

候选排序分数高，不等于可以自动写入。建议输出以下三个字段：

| 字段 | 含义 |
| --- | --- |
| rank_score | 只用于候选排序，尽量保证正确答案排在第一 |
| decision | auto_accept、review、no_match 或 user_confirmed |
| decision_reasons | 例如 ISRC 一致、主艺人一致、版本冲突、前两名分差不足 |

首版决策门槛建议：

| 决策 | 推荐条件 |
| --- | --- |
| auto_accept | ISRC 一致；或 rank_score ≥0.92、第一二名分差 ≥0.08、主艺人高匹配、无版本冲突 |
| review | 0.72–0.92；或前两名分差不足；或专辑/时长/版本存在矛盾；或来源字段不完整 |
| no_match | 最高分低于 0.72，或出现主艺人/版本硬冲突 |
| user_confirmed | 用户明确选定的候选；永久区别于算法 exact |

以上数字必须用标注集校准。上线初期宜偏保守：把边界样本放入 review，而不要为了提升匹配数量降低 auto_accept 门槛。

#### D. 提升召回而不牺牲精度

针对每个唯一曲目采用按需展开的查询预算：

| 顺序 | 策略 | 默认候选数 | 触发条件 |
| --- | --- | --- | --- |
| 0 | 本地人工确认缓存、长期精准缓存 | 1 | 有完全相同的来源 ID 或元数据指纹 |
| 1 | ISRC 查询/验证 | 1–3 | 来源包含 ISRC |
| 2 | 核心歌名 + 主艺人 | 12 | 所有正常曲目 |
| 3 | 核心歌名 + 其他艺人分隔变体、合作艺人 | 8–12 | 第 2 层未达到安全决策 |
| 4 | 核心歌名、别名、译名或罗马音 | 10–15 | 前两层无可靠候选或分差不足 |

第 2、3、4 层的结果应合并去重后统一评分。不要沿用“任一候选达到 0.60 就停止”的条件；只有在已满足 auto_accept 的全部规则时，才跳过后续层。这样，大部分明确曲目仍只有一次请求，只有真正有歧义的曲目才消耗额外请求预算。

对于艺人和标题变体，应优先做确定性的规则归一化，例如全半角、Unicode、常见分隔符、feat/featuring、and/&、顿号、括号内容的结构化保留。译名和罗马音可以作为低优先级召回词，不能因为字面相近而跨过主艺人和版本规则。

### 速度优化：减少无效请求，再安全地并发

#### A. 在进入网络前去重

同一歌单常有重复歌曲、不同歌单也常反复导入相同曲目。当前匹配任务按原始列表逐条提交，重复曲目会重复请求目录。

建议在 MatchingEngine 之前构建 stable_track_key：

- 优先键：来源平台 + source_id。
- 次优键：storefront + title_core + 主艺人规范化值 + album_normalized + 时长桶。
- 每个唯一键只匹配一次，再把同一个结果映射回原歌单的所有位置。

这项改动不会降低精度，且对含重复歌曲的歌单可直接减少相同数量的 API 调用。

#### B. 两级缓存与请求合并

| 层级 | 内容 | 失效策略 | 性能作用 |
| --- | --- | --- | --- |
| L1 内存 LRU | 最近的目录搜索结果和候选特征 | 短 TTL，例如 15–60 分钟 | 当前会话内的零网络复用 |
| L2 持久缓存 | 查询结果、规则版本、人工确认映射 | 目录结果按天级 TTL；人工确认可长期保存并撤销 | 重启后复跑和批量导入加速 |
| Singleflight | 同一时刻相同查询仅发起一条 HTTP 请求 | 请求完成后释放等待者 | 消除并发下的缓存穿透 |

缓存键至少包含 storefront、规范化查询、候选数量上限和 matcher_version。较大候选集的缓存可服务较小上限请求；反过来不可复用，避免截断导致误判。

空结果与失败也应分开缓存：

- 确认的无结果可短暂缓存，避免反复查询。
- 429、超时、认证错误和解析错误不得标记为无结果。
- 人工选择映射应带上来源元数据指纹；当歌名或时长变化明显时必须重新确认。

#### C. 统一并发和自适应限流

Web 端目前顺序发送分块请求，后端再在每块内部开 worker。后续不应仅把前端 CONCURRENCY 从 1 改大，因为这会与后端 worker 乘法叠加，难以控制实际 Apple API 并发。

推荐调度模型：

~~~mermaid
flowchart LR
    A[唯一曲目队列] --> B[匹配 worker]
    B --> C[全局 Catalog 请求队列]
    C --> D[令牌桶 + 并发上限]
    D --> E[Apple Music API]
    E --> F[缓存与候选合并]
    F --> G[评分 / 决策]
~~~

具体规则：

1. 只在 AppleMusicClient 一处控制 Catalog HTTP 的最大在途数与请求速率；匹配 worker 数不能绕开该限制。
2. 从 4 个在途目录请求作为保守起点，根据真实的 P95 延迟、429 比率和 token 情况逐步校准，而不是写死为最终值。
3. 发生 429 时，读取服务端重试提示；没有提示时采用指数退避加随机抖动，并临时把全局并发降为 1。
4. 连续成功一段窗口后，每次只增加 1 个并发位；不要在单次成功后立即恢复高并发。
5. 超时、5xx 和网络中断进入有限次数的重试队列；认证、参数和区域错误直接给出可解释状态。
6. 按请求而非按歌单统计限流。这样大歌单可以慢速继续，小歌单也不会被一次性任务完全阻塞。

#### D. 降低每首曲目的网络成本

- 严格查询优先、宽松查询延迟执行。只有严格层未产出安全决策才继续调用下一层。
- 候选的清洗、解析和评分全部在本地完成；同一候选不应为不同查询重复处理。
- 复用 HTTP 连接池，但不要假定跨大量线程共享的 requests Session 一定安全；实施时应通过受控的客户端封装或每 worker 会话保证并发行为可预测。
- 手动搜索接口应移出 FastAPI 事件循环中的同步网络调用，避免用户在搜索时冻结其他请求。
- 进度应按 已完成唯一曲目数 / 总唯一曲目数 计算，同时显示总原始曲目数，避免去重后用户误以为有曲目漏处理。

### 建议的请求与延迟预算

以下为验收目标，不是本次已测得的结果：

| 场景 | 请求预算 | 目标表现 |
| --- | --- | --- |
| 人工确认缓存命中 | 0 次目录请求 | 本地即时返回 |
| L1/L2 目录缓存命中 | 0 次目录请求 | 显著低于网络请求时延 |
| 正常明确曲目 | 1 次目录请求 | 严格层即可作出安全决定 |
| 合作/译名/同名曲 | 2–3 次目录请求 | 以更高精度换取有限额外时延 |
| 网络受限或异常 | 有限重试，不计入无匹配 | 可恢复或给出明确失败原因 |
| 含重复项的大歌单 | 按唯一曲目数请求 | 重复项不放大 API 压力 |

衡量速度时必须同时记录：每个唯一曲目的 API 请求数、缓存命中率、排队时间、网络时间、评分时间、重试次数和最终决策。只看整单耗时会掩盖缓存、限流和错误分类问题。

### 最小实现顺序

为了以最低风险取得速度和精度收益，建议按下列顺序实施：

1. 统一阈值和 decision 状态；修复缺字段中性分、手工选择伪 exact、0.60 提前停止。
2. 加入稳定曲目去重、L1 缓存和同查询 singleflight；这些优化不改变算法语义，风险最低。
3. 增加版本标签、专辑与时长的动态评分、第一二名分差和复核队列。
4. 引入持久缓存、ISRC 验证路径和分层查询预算。
5. 最后接入统一的自适应请求调度器，并以标注集和线上指标校准并发、候选数与阈值。

### 请求调度

- 保留连接复用，但把并发度改为由 429、超时率和近期延迟驱动的动态上限，而不是固定线程数或固定 200 ms 间隔。
- 429 时优先遵从服务端提供的重试建议；无建议时采用带随机抖动的指数退避，并协调暂停全部搜索 worker。
- 区分可重试错误（超时、429、部分 5xx）与不可重试错误（认证、参数、区域不可用）；不能一律返回空列表。
- 手动搜索也应通过后台线程或异步封装，避免同步 HTTP 调用阻塞 FastAPI 事件循环。
- 大歌单采用有限队列与背压，而不是一次性提交全部 future；可随限流状态降低并发。

### 缓存与反馈闭环

建议引入持久化缓存，例如 SQLite，并按以下键保存：

- storefront + 规范化查询 + 检索策略版本
- source + source_track_id + 元数据指纹
- 已人工确认的 来源曲目 → Apple Music Catalog ID 映射

缓存记录还应包含命中时间、候选摘要、结果状态、API 错误类别和规则版本。目录搜索可短期缓存；人工确认映射可长期保留并允许用户撤销；认证失败绝不能作为无搜索结果缓存。

### Web/CLI 交互

- 展示原始歌名、核心歌名、版本标签、候选歌名、专辑、时长差、匹配依据和警告，而不仅是一个百分比。
- 手工选择后状态改为 user_confirmed，保存确认时间和候选 ID；不要伪装成算法 exact。
- 将前两名过于接近、版本不一致、其他地区才有的条目排在复核队列前面。
- 允许总是选择该映射和此类版本不要自动选择，让人工复核转化为后续导入的高质量缓存。
- 导出的 CSV 增加决策来源、版本标签、第一二名分数、评分明细和 storefront，便于问题回溯。

## 实施路线图

| 阶段 | 工作内容 | 预期收益 | 验收条件 |
| --- | --- | --- | --- |
| 0：基线与观测 | 记录现有查询、候选、评分、最终选择和 HTTP 状态；建立人工标注样本 | 有可比较基线，避免盲目调参 | 可统计自动采纳准确率、召回率、P95 延迟、429 率 |
| 1：P0 正确性 | 版本解析；统一置信度配置；取消 0.60 过早停止；引入分差规则 | 显著减少 Live/Remix/同名误导入 | 版本类样本零自动错配，自动采纳精度达到目标 |
| 2：P1 召回与排序 | ISRC 验证路径、分层查询、候选扩容、专辑/时长/版本重排、艺人解析 | 提高跨平台和复杂曲目匹配率 | 标注集覆盖率提升，自动采纳精度不下降 |
| 3：P2 可靠性与体验 | 动态限流、持久缓存、失败分类、人工确认闭环、透明 UI | 大歌单更稳定，复跑更快，复核更少 | 429/超时可恢复；重复导入命中缓存；人工决策可复用 |
| 4：灰度发布 | 新旧策略并行评分，先只提示再自动采纳 | 将规则风险限制在可回滚范围 | 指标连续稳定后将新策略设为默认 |

## 回归数据集与质量指标

### 样本覆盖

至少建立 300 首人工确认样本，并按来源、地区和复杂度分层：

- 普通中文/英文歌曲
- 同名不同艺人
- 合作曲及多艺人
- Live、Remix、Acoustic、伴奏、Cover、Demo、Remaster
- 短标题（1–3 个字符）与纯符号/数字标题
- 中英文译名、罗马音、大小写与全半角差异
- 专辑版、单曲版、Explicit/Clean、时长差显著的版本
- 各来源字段完整与字段缺失场景
- 当前 storefront 无结果、其他 storefront 有结果的场景

### 指标

| 指标 | 定义 | 建议目标 |
| --- | --- | --- |
| 自动采纳精度 | 自动选择中实际正确的比例 | 先达到 ≥99%，再依据样本量提高目标 |
| 总匹配召回 | 有正确 Apple Music 结果的曲目中被找回的比例 | 相对现有基线持续提高 |
| 错版本率 | 应为原版/Live/Remix 等却命中错误版本的比例 | P0 样本应接近 0 |
| 人工复核率 | 需要用户决定的比例 | 在不牺牲精度前提下逐步下降 |
| P50/P95 单曲时延 | 从开始检索到决策的时间 | 按策略、歌单大小监控 |
| 429 与可恢复失败率 | 搜索受限或重试后仍失败的比例 | 降低且可解释 |
| 缓存命中率 | 可复用检索/确认结果的比例 | 大歌单复跑显著提升 |

## 预计改动面（供后续实施参考）

本次没有实施以下改动；仅列出后续落点，确保方案与现有结构对齐。

| 文件/模块 | 后续职责变化 |
| --- | --- |
| applemusic/models.py | 增加规范化元数据、版本标签、决策来源、评分明细和人工确认状态 |
| applemusic/extractors/*.py | 尽可能保留 ISRC、专辑、时长、原始艺人串和来源曲目 ID |
| applemusic/matcher/cleaner.py | 从删除噪声转为解析核心标题、版本和艺人角色 |
| applemusic/matcher/engine.py | 实现分层召回、候选合并、停止策略和跨 storefront 策略 |
| applemusic/matcher/scorer.py | 使用版本、专辑、ISRC、分差等特征，输出可解释决策 |
| applemusic/client.py | 扩充 Catalog 元数据、失败分类、动态限流和持久缓存接口 |
| applemusic/config.py | 集中定义策略版本、阈值、缓存 TTL、并发和 fallback storefront 开关 |
| applemusic/web/app.py | 让手动搜索走非阻塞调用并返回真实候选评分 |
| applemusic/web/static/index.html | 展示解释信息，区分算法匹配和人工确认 |
| applemusic/cli.py | 复用统一置信度与决策文案，输出可审计报告 |

## 推荐的最小可行版本

若只做一轮低风险优化，建议优先完成四项：

1. 保留 Live、Remix、Acoustic、Instrumental、Cover 等版本标签，并加入硬冲突规则。
2. 将停止条件从任意候选 ≥0.60 改为满足自动采纳条件；否则继续标题兜底查询。
3. 扩大候选集，用主艺人、专辑、时长和第一二名分差做统一排序。
4. 统一置信度阈值，手工选择标记为人工确认而非算法精确命中。

这四项能优先降低错误歌曲被自动写入 Apple Music 歌单的风险。之后再扩展 ISRC、持久缓存、多地区和动态限流，可在较低风险下继续提升覆盖率与速度。
