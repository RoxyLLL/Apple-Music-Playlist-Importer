# Antigravity执行指令

请在D:\applemusic实现本任务，依次完整阅读本目录SPEC.md、EVIDENCE.md、IMPLEMENTATION_PLAN.md和TEST_MATRIX.md。

本任务来自「灰かぶり（灰姑娘）— 十明」被误配为李杰明歌曲并显示89%高可信的截图。当前源码最小离线样例只有0.076/no_match，真实89%的运行条件未知；请明确诊断边界，不能编造复现或只给这两首歌加黑名单。

负责具体代码及测试：
1. 增加单曲可追溯诊断。
2. 构建有来源、艺人作用域的多语言查询与证据。
3. 修正转写/子串/Single+时长等弱证据自动采纳。
4. 让初次、深度、缓存和个人资料库相关匹配都遵守证据门槛。
5. 匹配缓存版本化，重新评估旧自动结果，保留正确限定范围的人工确认。
6. 前端统一decision徽标与默认选择，分数不伪装概率。
7. 完成强制矩阵、已有回归和前端行为验证，写RESULT.md。

优先修复误采纳与旧缓存，再补可靠召回。仅增加十明别名或抬高总分阈值不满足方案。
引用官方作品译名Cinder ella时必须限定艺人/作品；拉丁艺名需核实来源。测试用的合成时长/ISRC必须标注，不写入真实曲库别名事实。
不得读取/输出真实Token，不操作真实资料库、不清理整个用户缓存、不下载音频。使用临时用户目录、临时缓存、虚拟凭据、未mock HTTP阻断和外部进程mock。
不覆盖上一轮已验收功能，不提交/推送/打Tag/发布。若已知样例的旧断言与新契约冲突，写明变更理由，不能通过删除或跳过测试交付。
完成后在RESULT.md记录修改清单、前后矩阵、缓存兼容、预算、独立复现命令、前端验证证据、未验证项目和遗留风险，等待Codex验收。

