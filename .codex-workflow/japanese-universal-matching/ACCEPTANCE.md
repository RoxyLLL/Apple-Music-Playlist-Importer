# 复验报告

## 结论

**通过（线上效果待真实样本验证）。** 上轮 2 项阻断均已修复。

## 验收结果

- 旧 `match_cache` 可兼容含 `reasons`、缺失版本字段的历史 JSON；旧 `auto_accept` 会按 v2 重评，`user_confirmed` 保留。
- 合成评估已明确标为 **Synthetic Benchmark**，不再宣称真实 Apple Music 曲库达到 100% 召回。
- 未发现本轮新增的单曲/艺人特例硬编码；方案仍以通用归一化、查询扩展、跨区等价映射和保守证据判定为主。
- 专项测试：**45/45 通过**。
- 全量回归：**171/171 通过**。
- `compileall`、`git diff --check`：通过。

## 非阻断项

- 尚未使用真实 Apple Music 在线响应验证“几乎所有日语歌”的实际 Recall@20；当前 200 正例、100 困难负例和 30 跨区样本均为合成数据。
- 测试仍出现 1 处 Pydantic v2 `dict()` 弃用警告，和本功能正确性无关；实现报告中的“0 警告”表述不准确。
